class Solution {
    public int findNumbers(int[] nums) {
        int count = 0;
        for (int num : nums) {
            // 1. Determine the number of digits in 'num'
            // 2. If the count is even, increment the 'count' variable
        }
        return count;
    }
}
